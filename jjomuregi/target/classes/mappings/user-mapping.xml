<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserDAO">
	<insert id="insertUser" parameterType="user">
		INSERT INTO USERS(id,pw,nickname) VALUES(#{id},#{pw},#{nickName})
	</insert>
	<update id="updateUser" parameterType="user">
		UPDATE USERS SET PASSWORD=#{pw} where ID=#{id}
	</update>
	<delete id="deleteUser" parameterType="user">
		DELETE FROM USERS WHERE ID=#{id}
	</delete>
	<select id="getUser" parameterType="user" resultType="user">
		SELECT * FROM USERS WHERE ID=#{id}
	</select>
	<select id="getNickNameUser" parameterType="user" resultType="user">
		SELECT * FROM USERS WHERE NICKNAME=#{nickName}
	</select>
	<select id="confirmUser" parameterType="user" resultType="int">
		SELECT NVL(COUNT(*),0) FROM USERS WHERE ID=#{id}
	</select>
</mapper>
